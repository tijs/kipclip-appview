# Local Development Environment Variables
# Copy this file to .env and fill in your values

# Base URL for your application
# Deno Deploy production: https://kipclip.com
# Local development: http://127.0.0.1:8000 (use 127.0.0.1, not localhost - RFC 8252)
BASE_URL=http://127.0.0.1:8000

# Cookie secret for session encryption
# IMPORTANT: Generate a secure random string (minimum 32 characters)
# You can generate one with: openssl rand -base64 32
COOKIE_SECRET=your-secure-random-secret-minimum-32-chars-long

# Encryption key for sensitive user data (e.g., Instapaper credentials)
# IMPORTANT: Generate a secure random string (minimum 32 characters)
# You can generate one with: openssl rand -base64 32
ENCRYPTION_KEY=your-secure-encryption-key-minimum-32-chars-long

# Optional: Custom port for local dev server (default: 8000)
# PORT=8000

# Turso/libSQL Database
# For local development: leave unset (uses file:.local/kipclip.db)
# For production: use your Turso database URL
# TURSO_DATABASE_URL=libsql://your-db.turso.io
# TURSO_AUTH_TOKEN=your-auth-token

# Sentry Error Tracking (optional)
# Get your DSN from: Settings → Projects → [project] → Client Keys (DSN)
# SENTRY_DSN=https://xxx@xxx.ingest.sentry.io/xxx
